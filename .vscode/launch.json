{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Iniciar o Chrome em relação a localhost",
      "url": "http://localhost/site/",
      "webRoot": "${workspaceFolder}"
    }
  ]
}